# Design System Architecture Overview

## System Context

```
┌─────────────────────────────────────────────────────────────────┐
│                    @thamimagi/design-system                      │
│              (Framework-agnostic UI Components)                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │  Astro Blog  │  │  Directus    │  │  Future      │           │
│  │   (React)    │  │    (Vue)     │  │   Apps       │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
│         │                │                │                      │
│         └────────────────┼────────────────┘                      │
│                          │                                       │
│                          ▼                                       │
│                  ┌──────────────┐                                │
│                  │   DaisyUI    │                                │
│                  │   (CSS)      │                                │
│                  └──────────────┘                                │
│                          │                                       │
│                          ▼                                       │
│                  ┌──────────────┐                                │
│                  │ TailwindCSS  │                                │
│                  │     v4       │                                │
│                  └──────────────┘                                │
└─────────────────────────────────────────────────────────────────┘
```

## Package Architecture

```
@thamimagi/design-system/
├── dist/                    # Built output
│   ├── index.js             # ESM bundle
│   ├── index.d.ts           # TypeScript declarations
│   └── styles.css           # Compiled CSS
├── src/
│   ├── components/          # React components
│   │   ├── Button/
│   │   │   ├── Button.tsx
│   │   │   ├── Button.stories.tsx
│   │   │   └── index.ts
│   │   ├── Card/
│   │   └── index.ts         # Barrel export
│   ├── styles/
│   │   └── globals.css      # Tailwind/DaisyUI imports
│   ├── utils/
│   │   └── cn.ts            # className utility
│   └── index.ts             # Main entry
└── package.json
```

## Component Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                      Consumer Application                        │
│  import { Button } from "@thamimagi/design-system"              │
├─────────────────────────────────────────────────────────────────┤
│                      Component Layer                             │
│  React/Vue/Svelte components with typed props                   │
├─────────────────────────────────────────────────────────────────┤
│                      Styling Layer                               │
│  DaisyUI semantic classes (btn, card, input, etc.)              │
├─────────────────────────────────────────────────────────────────┤
│                      Utility Layer                               │
│  TailwindCSS v4 utility classes                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Build Pipeline

```
Source (TypeScript/React)
        │
        ▼
┌───────────────┐
│     Vite      │ ─── TypeScript compilation
│    Build      │ ─── React JSX transform
└───────┬───────┘ ─── CSS processing
        │
        ▼
┌───────────────┐
│    Output     │
│   - dist/     │ ─── ESM bundle + types
│   - styles    │ ─── Compiled CSS
└───────────────┘
```

## Multi-Framework Strategy

| Framework | Approach | Status |
|-----------|----------|--------|
| React | Native components | ✅ Primary |
| Vue | Native components | 🔄 Planned |
| Svelte | Native components | 🔄 Planned |
| Web Components | Wrapper layer | 🔮 Future |

### Shared Foundation

All framework variants share:
- Same DaisyUI class foundation
- Same visual design
- Same accessibility patterns
- Same theming system

## Technology Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| CSS Framework | TailwindCSS v4 | Modern, utility-first |
| Component Classes | DaisyUI v5 | Semantic, framework-agnostic |
| Primary Framework | React 19 | Widest adoption |
| Bundler | Vite | Fast, modern ESM output |
| Documentation | Storybook 8 | Industry standard |
| Package Manager | pnpm | Fast, strict |

## Publishing

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   GitHub     │────>│   GitHub     │────>│     npm      │
│   Push       │     │   Actions    │     │   Registry   │
└──────────────┘     └──────────────┘     └──────────────┘
                             │
                             ▼
                     ┌──────────────┐
                     │   GitHub     │
                     │   Pages      │
                     │ (Storybook)  │
                     └──────────────┘
```
